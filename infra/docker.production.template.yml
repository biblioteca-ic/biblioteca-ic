version: "3.6"

services:
        backend:
                image: registry.gitlab.com/infra103/biblioteca-ic/backend:${IMAGE_TAG}
        frontend:
                image: registry.gitlab.com/infra103/biblioteca-ic/frontend:${IMAGE_TAG}
