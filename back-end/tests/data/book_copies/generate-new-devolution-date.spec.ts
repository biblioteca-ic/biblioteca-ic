describe('GenerateNewDevolutionDate', () => {
  test.todo('should return, if new date is smaller than devolution limit, the new date')
  test.todo('should return, if new date is greater than devolution limit, the devolution limit')
  test.todo('should return a Date on success')
})