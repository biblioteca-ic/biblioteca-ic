describe('CheckUserPendencies', () => {
  test.todo('should return undefined if doesn\'t have copies.')
  test.todo('should return a BadRequestError if have three or more copies.')
  test.todo('should return a BadRequestError if have some overdue copy.')
  test.todo('should return a BarRequestError if a requested book is equal to another current borrowed book.')
  test.todo('should return undefined if is ok.')
})